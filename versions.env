# =============================================================================
# 动态版本管理配置文件
# =============================================================================
# 此文件是所有版本信息的单一数据源，Docker 构建过程会自动读取这些版本号
# 修改此文件后，后续的 Docker 构建将自动使用新的版本号，无需手动更新 Dockerfile
#
# 版本格式要求：
# - NODE_VERSION: 必须是有效的 Node.js 版本号（数字，如：16, 18, 20）
# - CLAUDE_*_VERSION: 必须符合语义版本格式（x.y.z，如：1.0.54）
# - VERSION: 必须以 'v' 开头的版本标签（如：v1.0.0）
# - IMAGE_NAME: 必须是有效的 Docker 镜像名称（小写字母、数字、连字符）
# =============================================================================

# Docker 镜像版本
# 格式: v主版本.次版本.修订版本
VERSION=v1.0.0

# Docker 镜像名称
# 格式: 小写字母、数字、连字符组合
IMAGE_NAME=cloud-code-dev

# NPM 包版本
# 格式: 主版本.次版本.修订版本（语义版本）
CLAUDE_CODE_VERSION=1.0.54
CLAUDE_ROUTER_VERSION=1.0.26

# Node.js 版本
# 格式: 主版本号（数字）
NODE_VERSION=20
