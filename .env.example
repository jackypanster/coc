# 企业SSO配置示例
# 复制此文件为 .env 并填入实际配置值

# 企业SSO配置（敏感信息，不应提交到Git）
# 请根据实际环境填写以下配置
SSO_OAUTH_URL=https://your-sso-domain.com/login
SSO_TOKEN_URL=https://your-sso-domain.com/oauth/token
SSO_USERINFO_URL=https://your-sso-domain.com/oauth/userinfo
SSO_CLIENT_ID=your-client-id
SSO_CLIENT_SECRET=your-client-secret
SSO_REDIRECT_URI=https://your-domain.com/login/callback

# 环境配置
NODE_ENV=development

# 生产环境示例：
# NODE_ENV=production
# SSO_REDIRECT_URI=https://your-domain.com/login/callback
# GFT_REDIRECT_URI=https://your-domain.com/login/callback
